name: Update Statistics Dashboard

on:
  schedule:
    - cron: '0 3 * * *'  # Daily at 3 AM UTC
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    environment: github-pages
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements-dashboard.txt

      - name: Generate statistics
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          ARCHIVE_PATH: ./archives
          DB_SERVER_IP: ${{ secrets.DB_SERVER_IP }}
          DB_SERVER_SSH_KEY: ${{ secrets.DB_SERVER_SSH_KEY }}
        run: |
          # Set up SSH key if provided
          if [ -n "$DB_SERVER_SSH_KEY" ]; then
            mkdir -p ~/.ssh
            echo "$DB_SERVER_SSH_KEY" > ~/.ssh/db_server_key
            chmod 600 ~/.ssh/db_server_key
            export DB_SERVER_SSH_KEY=~/.ssh/db_server_key
          fi
          python -u scripts/generate_stats.py

      - name: Build with Jekyll
        uses: actions/jekyll-build-pages@v1
        with:
          source: ./docs
          destination: ./_site

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./_site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
